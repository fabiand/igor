#
# A simple plan for a basic TUI and auto-simple
# Variables have the format {[^}]+}
#

# Testsuite             Profile         Host                Optional: kargs
basic_tui_installation  {profile_pri}   default-libvirt

basic                   {profile_pri}   default-libvirt     kargs='storage_init BOOTIF=link'
extended                {profile_pri}   default-libvirt     kargs='storage_init BOOTIF=link'
